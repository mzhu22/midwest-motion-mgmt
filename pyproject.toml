[project]
name = "midwest-motion-mgmt"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "flask-cors>=4.0",
    "SimpleITK>=2.3",
    "numpy>=1.24",
    "Pillow>=10.0",
    "scipy>=1.11",
]

[dependency-groups]
dev = ["pytest>=8.0"]
